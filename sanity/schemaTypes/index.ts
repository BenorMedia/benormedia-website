// Sanity schema types registry.
//
// Intentionally empty for Phase 0 (scaffold-only). Real document, object, and
// singleton schemas land in Phase 2 per docs/SCHEMAS.md.
//
// When adding schemas later, import each from ./documents, ./objects, or
// ./singletons and append to this array.
export const schemaTypes: never[] = [];
